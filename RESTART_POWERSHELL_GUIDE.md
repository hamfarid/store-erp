# 🔄 دليل إعادة تشغيل PowerShell
# PowerShell Restart Guide

**التاريخ:** 2025-10-08 13:30  
**الحالة:** Node.js مثبت ✅ لكن PowerShell يحتاج إلى إعادة تشغيل

---

## ✅ الأخبار الجيدة

```
✅ Node.js مثبت بنجاح!
✅ تم التأكيد من winget
✅ الملفات موجودة في: C:\Program Files\nodejs\
```

---

## ⚠️ المشكلة

```
❌ PowerShell الحالي لا يتعرف على npm
💡 السبب: PATH لم يتم تحديثه في الجلسة الحالية
💡 الحل: إعادة فتح PowerShell
```

---

## 🎯 الحل (خطوة واحدة فقط!)

### **أغلق PowerShell الحالي وافتح واحد جديد**

1. **أغلق PowerShell الحالي:**
   - اضغط على ❌ لإغلاق النافذة
   - أو اكتب `exit` واضغط Enter

2. **افتح PowerShell جديد:**
   - اضغط `Win + X`
   - اختر "Windows PowerShell" أو "Terminal"

3. **انتقل إلى مجلد frontend:**
   ```powershell
   cd D:\APPS_AI\store\store_v1.6\frontend
   ```

4. **تحقق من npm:**
   ```powershell
   npm --version
   ```
   
   **يجب أن ترى:** `10.x.x` أو `9.x.x` ✅

5. **ثبّت المتطلبات:**
   ```powershell
   npm install
   ```
   
   **الوقت المتوقع:** 2-5 دقائق

6. **شغّل Frontend:**
   ```powershell
   npm run dev
   ```

7. **افتح المتصفح:**
   ```
   http://localhost:5502
   ```

---

## 📝 الأوامر الكاملة (نسخ ولصق)

**في PowerShell الجديد:**

```powershell
cd D:\APPS_AI\store\store_v1.6\frontend
npm --version
npm install
npm run dev
```

---

## 🔍 ماذا تتوقع؟

### بعد `npm --version`:
```
10.2.4
```

### بعد `npm install`:
```
added 1234 packages, and audited 1235 packages in 2m

123 packages are looking for funding
  run `npm fund` for details

found 0 vulnerabilities
```

### بعد `npm run dev`:
```
  VITE v7.0.4  ready in 1234 ms

  ➜  Local:   http://localhost:5502/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

---

## ⚡ إذا لم ينجح

### الحل البديل 1: إعادة تشغيل الكمبيوتر

```
1. أعد تشغيل الكمبيوتر
2. افتح PowerShell
3. cd D:\APPS_AI\store\store_v1.6\frontend
4. npm install
5. npm run dev
```

### الحل البديل 2: استخدام السكريبت

```powershell
# في PowerShell جديد كمسؤول:
cd D:\APPS_AI\store\store_v1.6
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\refresh-and-install.ps1
```

---

## 📊 الحالة الحالية

```
╔════════════════════════════════════════════════════════════╗
║                                                            ║
║  ✅ Node.js: مثبت                                         ║
║  ✅ Backend: يعمل (http://127.0.0.1:5002)                ║
║  ⏳ npm: يحتاج PowerShell جديد                            ║
║  ⏳ Frontend: بانتظار npm install                         ║
║                                                            ║
║  🎯 الخطوة التالية:                                       ║
║     أغلق PowerShell وافتح واحد جديد                      ║
║                                                            ║
╚════════════════════════════════════════════════════════════╝
```

---

## 🎉 بعد النجاح

**ستتمكن من:**
- ✅ الوصول إلى Frontend على http://localhost:5502
- ✅ تسجيل الدخول (admin / admin123)
- ✅ استخدام جميع الميزات
- ✅ التفاعل مع Backend

---

**🚀 فقط أغلق PowerShell الحالي وافتح واحد جديد!**

**📝 Node.js مثبت ويعمل - فقط يحتاج PowerShell جديد لتحديث PATH**

