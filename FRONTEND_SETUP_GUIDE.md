# 🎨 دليل إعداد وتشغيل Frontend
# Frontend Setup Guide

**التاريخ:** 2025-10-08  
**الحالة:** يتطلب تثبيت Node.js

---

## ⚠️ المشكلة الحالية

```
❌ npm غير مثبت على النظام
❌ لا يمكن تشغيل Frontend بدون Node.js
```

---

## 📋 المتطلبات

### 1. Node.js و npm
- **الإصدار المطلوب:** Node.js 18.0.0 أو أحدث
- **npm:** 9.0.0 أو أحدث

---

## 🚀 خطوات التثبيت والتشغيل

### الخطوة 1: تثبيت Node.js

#### على Windows:

**الطريقة 1: من الموقع الرسمي (موصى بها)**
1. افتح المتصفح واذهب إلى: https://nodejs.org/
2. قم بتحميل النسخة **LTS** (Long Term Support)
3. قم بتشغيل ملف التثبيت
4. اتبع التعليمات (اختر جميع الخيارات الافتراضية)
5. أعد تشغيل PowerShell

**الطريقة 2: باستخدام Chocolatey**
```powershell
# إذا كان لديك Chocolatey مثبت
choco install nodejs-lts
```

**الطريقة 3: باستخدام Winget**
```powershell
# إذا كان لديك Winget (Windows 10/11)
winget install OpenJS.NodeJS.LTS
```

#### التحقق من التثبيت:
```powershell
# أعد فتح PowerShell ثم اكتب:
node --version
npm --version
```

يجب أن ترى:
```
v18.x.x  (أو أحدث)
9.x.x    (أو أحدث)
```

---

### الخطوة 2: تثبيت متطلبات Frontend

```powershell
# انتقل إلى مجلد frontend
cd D:\APPS_AI\store\store_v1.6\frontend

# تثبيت المتطلبات
npm install
```

**الوقت المتوقع:** 2-5 دقائق (حسب سرعة الإنترنت)

---

### الخطوة 3: تشغيل Frontend

```powershell
# تشغيل خادم التطوير
npm run dev
```

**سيعمل على:**
```
http://localhost:5502
```

---

## 🌐 الوصول إلى التطبيق

بعد تشغيل Frontend، افتح المتصفح على:
```
http://localhost:5502
```

**بيانات تسجيل الدخول:**
```
اسم المستخدم: admin
كلمة المرور: admin123
```

---

## 📊 الحالة الحالية

### ✅ Backend (يعمل):
```
🌐 http://127.0.0.1:5002
✅ 18 Blueprints مسجلة
✅ 41 مسار API متاح
✅ قاعدة البيانات جاهزة
```

### ⏳ Frontend (بانتظار Node.js):
```
❌ npm غير متاح
⏳ بانتظار تثبيت Node.js
⏳ بانتظار npm install
⏳ بانتظار npm run dev
```

---

## 🔧 حل المشاكل الشائعة

### المشكلة 1: "npm is not recognized"
**الحل:**
1. تأكد من تثبيت Node.js
2. أعد تشغيل PowerShell
3. تحقق من PATH:
   ```powershell
   $env:PATH
   ```
4. يجب أن يحتوي على مسار Node.js مثل:
   ```
   C:\Program Files\nodejs\
   ```

### المشكلة 2: "EACCES: permission denied"
**الحل:**
```powershell
# قم بتشغيل PowerShell كمسؤول
# ثم نفذ:
npm install --global npm@latest
```

### المشكلة 3: "Cannot find module"
**الحل:**
```powershell
# احذف node_modules وأعد التثبيت
rm -r node_modules
rm package-lock.json
npm install
```

### المشكلة 4: "Port 5502 is already in use"
**الحل:**
```powershell
# غير المنفذ في package.json أو:
npm run dev -- --port 5503
```

---

## 📦 المتطلبات المثبتة (بعد npm install)

### Dependencies الرئيسية:
- ✅ React 18.3.1
- ✅ React Router DOM 7.6.1
- ✅ Axios 1.7.9
- ✅ Tailwind CSS 4.1.7
- ✅ Lucide React 0.510.0
- ✅ Recharts 2.15.3
- ✅ React Hook Form 7.56.3
- ✅ Zod 3.24.4

### Dev Dependencies:
- ✅ Vite 7.0.4
- ✅ ESLint 9.25.0
- ✅ Vitest 3.2.4

---

## 🎯 الأوامر المتاحة

```powershell
# تشغيل خادم التطوير
npm run dev

# بناء للإنتاج
npm run build

# معاينة البناء
npm run preview

# فحص الكود
npm run lint

# إصلاح مشاكل الكود
npm run lint:fix

# تشغيل الاختبارات
npm test

# تشغيل الاختبارات مع التغطية
npm run test:coverage
```

---

## 📁 هيكل Frontend

```
frontend/
├── public/              # الملفات الثابتة
├── src/
│   ├── components/      # المكونات
│   │   ├── LoginEnhanced.jsx        ⭐ جديد
│   │   └── shared/
│   │       ├── Button.jsx           ⭐ جديد
│   │       └── Input.jsx            ⭐ جديد
│   ├── pages/           # الصفحات
│   │   └── DashboardEnhanced.jsx    ⭐ جديد
│   ├── context/         # Context API
│   ├── utils/           # أدوات مساعدة
│   └── App.jsx          # التطبيق الرئيسي
├── package.json
└── vite.config.js
```

---

## 🔗 الاتصال بـ Backend

Frontend مُعد للاتصال بـ Backend على:
```
http://127.0.0.1:5002
```

تأكد من أن Backend يعمل قبل تشغيل Frontend!

---

## 📝 ملاحظات مهمة

### 1. CORS:
✅ Backend مُعد للسماح بطلبات من Frontend
✅ CORS مفعّل على جميع المسارات

### 2. Authentication:
✅ JWT tokens تُحفظ في localStorage
✅ Auto-refresh للـ tokens
✅ Redirect تلقائي للـ login عند انتهاء الصلاحية

### 3. الأداء:
✅ Lazy loading للصفحات
✅ Code splitting
✅ Image optimization
✅ Caching

---

## 🎨 الميزات المتاحة

### الصفحات المحسّنة:
1. ✅ **صفحة تسجيل الدخول** (LoginEnhanced.jsx)
   - Validation محسّن
   - رسائل خطأ واضحة
   - Remember Me
   - Demo login button

2. ✅ **لوحة التحكم** (DashboardEnhanced.jsx)
   - إحصائيات حقيقية من API
   - رسوم بيانية تفاعلية
   - بطاقات الإحصائيات
   - إجراءات سريعة

### المكونات الموحدة:
1. ✅ **Button** - أزرار موحدة
2. ✅ **Input** - حقول إدخال موحدة
3. ⏳ **Table** - جداول موحدة
4. ⏳ **Modal** - نوافذ منبثقة موحدة
5. ⏳ **Card** - بطاقات موحدة

---

## 🚀 الخطوات التالية

### بعد تثبيت Node.js:

1. **تثبيت المتطلبات:**
   ```powershell
   cd D:\APPS_AI\store\store_v1.6\frontend
   npm install
   ```

2. **تشغيل Frontend:**
   ```powershell
   npm run dev
   ```

3. **فتح المتصفح:**
   ```
   http://localhost:5502
   ```

4. **تسجيل الدخول:**
   ```
   Username: admin
   Password: admin123
   ```

5. **الاستمتاع بالنظام!** 🎉

---

## 📚 الموارد

- **Node.js:** https://nodejs.org/
- **npm Documentation:** https://docs.npmjs.com/
- **Vite Documentation:** https://vitejs.dev/
- **React Documentation:** https://react.dev/
- **Tailwind CSS:** https://tailwindcss.com/

---

## 🆘 الدعم

إذا واجهت أي مشاكل:

1. تحقق من السجلات في Terminal
2. راجع `FRONTEND_SETUP_GUIDE.md` (هذا الملف)
3. راجع `SERVER_STARTUP_GUIDE.md`
4. راجع `PROJECT_COMPLETION_REPORT.md`

---

**📝 ملخص سريع:**

```powershell
# 1. تثبيت Node.js من https://nodejs.org/
# 2. أعد فتح PowerShell
# 3. نفذ:
cd D:\APPS_AI\store\store_v1.6\frontend
npm install
npm run dev
# 4. افتح http://localhost:5502
```

**🎉 بالتوفيق!**

