# 📦 دليل تثبيت Node.js و npm
# Node.js & npm Installation Guide

**التاريخ:** 2025-10-08  
**الهدف:** تثبيت Node.js و npm لتشغيل Frontend

---

## 🎯 لماذا نحتاج Node.js؟

Frontend مبني باستخدام:
- ⚛️ **React 18.3.1** - مكتبة JavaScript
- ⚡ **Vite 7.0.4** - أداة البناء
- 🎨 **Tailwind CSS 4.1.7** - إطار CSS

كل هذه الأدوات تحتاج إلى **Node.js** و **npm** للعمل.

---

## 📋 المتطلبات

- **نظام التشغيل:** Windows 10/11
- **المساحة:** ~200 MB
- **الوقت:** 5-10 دقائق
- **الاتصال بالإنترنت:** مطلوب للتحميل

---

## 🚀 الطريقة 1: التحميل المباشر (موصى بها)

### الخطوة 1: فتح الموقع الرسمي

افتح المتصفح واذهب إلى:
```
https://nodejs.org/
```

### الخطوة 2: اختيار النسخة

ستجد زرين:
1. **LTS (Long Term Support)** - الزر الأخضر ← **اختر هذا** ✅
2. **Current** - الزر الرمادي ← لا تختر هذا

**النسخة الموصى بها:** v20.x.x أو v18.x.x

### الخطوة 3: التحميل

- اضغط على زر **LTS**
- سيتم تحميل ملف مثل: `node-v20.11.0-x64.msi`
- حجم الملف: ~30 MB تقريباً
- الوقت: 1-3 دقائق (حسب سرعة الإنترنت)

### الخطوة 4: التثبيت

1. **افتح الملف الذي تم تحميله**
   - انقر نقراً مزدوجاً على `node-v20.x.x-x64.msi`

2. **شاشة الترحيب**
   - اضغط **Next**

3. **اتفاقية الترخيص**
   - حدد "I accept the terms in the License Agreement"
   - اضغط **Next**

4. **مسار التثبيت**
   - اترك المسار الافتراضي: `C:\Program Files\nodejs\`
   - اضغط **Next**

5. **المكونات المخصصة**
   - **مهم جداً:** تأكد من تحديد:
     - ✅ Node.js runtime
     - ✅ npm package manager
     - ✅ **Add to PATH** ← هذا مهم جداً!
   - اضغط **Next**

6. **أدوات إضافية (اختياري)**
   - يمكنك تحديد "Automatically install the necessary tools"
   - أو تخطي هذه الخطوة
   - اضغط **Next**

7. **بدء التثبيت**
   - اضغط **Install**
   - انتظر 2-3 دقائق

8. **الانتهاء**
   - اضغط **Finish**

### الخطوة 5: التحقق من التثبيت

**مهم جداً:** أغلق أي PowerShell مفتوح وافتح واحد جديد!

```powershell
# افتح PowerShell جديد
node --version
npm --version
```

**النتيجة المتوقعة:**
```
v20.11.0  (أو أي إصدار 18.x أو 20.x)
10.2.4    (أو أي إصدار 9.x أو 10.x)
```

**إذا ظهرت الأرقام:** ✅ التثبيت نجح!  
**إذا ظهر خطأ:** ❌ راجع قسم "حل المشاكل" أدناه

---

## 🚀 الطريقة 2: باستخدام Winget (Windows 10/11)

إذا كان لديك Windows 10 (build 1809+) أو Windows 11:

### الخطوة 1: فتح PowerShell كمسؤول
```
انقر بزر الماوس الأيمن على قائمة Start
اختر "Windows PowerShell (Admin)" أو "Terminal (Admin)"
```

### الخطوة 2: تثبيت Node.js
```powershell
winget install OpenJS.NodeJS.LTS
```

### الخطوة 3: التحقق
أغلق PowerShell وافتح واحد جديد:
```powershell
node --version
npm --version
```

---

## 🚀 الطريقة 3: باستخدام Chocolatey

إذا كان لديك Chocolatey مثبت:

### الخطوة 1: فتح PowerShell كمسؤول

### الخطوة 2: تثبيت Node.js
```powershell
choco install nodejs-lts
```

### الخطوة 3: التحقق
```powershell
node --version
npm --version
```

---

## 🔧 حل المشاكل الشائعة

### المشكلة 1: "node is not recognized"

**السبب:** Node.js غير مضاف إلى PATH

**الحل:**

1. **إعادة تشغيل PowerShell**
   - أغلق جميع نوافذ PowerShell
   - افتح PowerShell جديد
   - جرب مرة أخرى

2. **إعادة تشغيل الكمبيوتر**
   - أحياناً يحتاج Windows لإعادة تشغيل

3. **إضافة يدوية إلى PATH**
   ```
   1. افتح "System Properties" (Win + Pause)
   2. اضغط "Advanced system settings"
   3. اضغط "Environment Variables"
   4. في "System variables"، ابحث عن "Path"
   5. اضغط "Edit"
   6. أضف: C:\Program Files\nodejs\
   7. اضغط OK
   8. أعد فتح PowerShell
   ```

### المشكلة 2: "Permission denied"

**الحل:**
```powershell
# افتح PowerShell كمسؤول
# ثم نفذ:
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### المشكلة 3: التثبيت فشل

**الحل:**
1. احذف Node.js من Control Panel
2. احذف المجلدات:
   - `C:\Program Files\nodejs\`
   - `C:\Users\[YourName]\AppData\Roaming\npm\`
3. أعد التثبيت

### المشكلة 4: npm بطيء جداً

**الحل:**
```powershell
# استخدم مرآة أسرع (مثل Taobao للصين)
npm config set registry https://registry.npmmirror.com
```

---

## ✅ بعد التثبيت الناجح

### الخطوة 1: الانتقال إلى مجلد Frontend
```powershell
cd D:\APPS_AI\store\store_v1.6\frontend
```

### الخطوة 2: تثبيت المتطلبات
```powershell
npm install
```

**الوقت المتوقع:** 2-5 دقائق  
**حجم التحميل:** ~200-300 MB

**ستشاهد:**
```
npm WARN deprecated ...
added 1234 packages in 2m
```

### الخطوة 3: تشغيل Frontend
```powershell
npm run dev
```

**ستشاهد:**
```
  VITE v7.0.4  ready in 1234 ms

  ➜  Local:   http://localhost:5502/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

### الخطوة 4: فتح المتصفح
```
http://localhost:5502
```

---

## 📊 التحقق من التثبيت الكامل

### اختبار Node.js:
```powershell
node --version
# يجب أن يظهر: v20.x.x أو v18.x.x
```

### اختبار npm:
```powershell
npm --version
# يجب أن يظهر: 10.x.x أو 9.x.x
```

### اختبار npx:
```powershell
npx --version
# يجب أن يظهر: 10.x.x أو 9.x.x
```

### اختبار تشغيل كود JavaScript:
```powershell
node -e "console.log('Hello from Node.js!')"
# يجب أن يظهر: Hello from Node.js!
```

---

## 🎯 الخطوات التالية

بعد تثبيت Node.js بنجاح:

1. ✅ **تثبيت متطلبات Frontend:**
   ```powershell
   cd D:\APPS_AI\store\store_v1.6\frontend
   npm install
   ```

2. ✅ **تشغيل Frontend:**
   ```powershell
   npm run dev
   ```

3. ✅ **فتح المتصفح:**
   ```
   http://localhost:5502
   ```

4. ✅ **تسجيل الدخول:**
   ```
   Username: admin
   Password: admin123
   ```

---

## 📚 موارد إضافية

- **الموقع الرسمي:** https://nodejs.org/
- **التوثيق:** https://nodejs.org/docs/
- **npm Documentation:** https://docs.npmjs.com/
- **دليل Frontend:** `FRONTEND_SETUP_GUIDE.md`
- **دليل الخوادم:** `SERVER_STARTUP_GUIDE.md`

---

## 🆘 الدعم

إذا واجهت أي مشاكل:

1. راجع قسم "حل المشاكل" أعلاه
2. تأكد من إعادة فتح PowerShell بعد التثبيت
3. جرب إعادة تشغيل الكمبيوتر
4. تحقق من أن PATH يحتوي على مسار Node.js

---

## ✅ قائمة التحقق

- [ ] تم تحميل Node.js من nodejs.org
- [ ] تم تثبيت Node.js بنجاح
- [ ] تم التحقق من `node --version`
- [ ] تم التحقق من `npm --version`
- [ ] تم إعادة فتح PowerShell
- [ ] تم الانتقال إلى مجلد frontend
- [ ] تم تنفيذ `npm install`
- [ ] تم تنفيذ `npm run dev`
- [ ] تم فتح http://localhost:5502
- [ ] تم تسجيل الدخول بنجاح

---

**🎉 بالتوفيق في التثبيت!**

**📝 ملاحظة:** التثبيت يستغرق 5-10 دقائق فقط، وهو ضروري لتشغيل أي تطبيق React/Vite.

