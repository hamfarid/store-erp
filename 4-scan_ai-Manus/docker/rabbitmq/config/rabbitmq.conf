# تكوين RabbitMQ المتقدم لنظام Gaara Scan AI

# إعدادات الشبكة والاتصال
listeners.tcp.default = 5672
management.tcp.port = 15672
management.tcp.ip = 0.0.0.0

# إعدادات الأمان
auth_backends.1 = rabbit_auth_backend_internal
auth_mechanisms.1 = PLAIN
auth_mechanisms.2 = AMQPLAIN

# إعدادات الذاكرة والأداء
vm_memory_high_watermark.relative = 0.6
vm_memory_high_watermark_paging_ratio = 0.5
disk_free_limit.relative = 2.0

# إعدادات الرسائل
default_vhost = gaara_vhost
default_user = gaara_admin
default_pass = gaara_rabbit_2024
default_permissions.configure = .*
default_permissions.read = .*
default_permissions.write = .*

# إعدادات الكتلة (Clustering)
cluster_formation.peer_discovery_backend = rabbit_peer_discovery_classic_config
cluster_formation.classic_config.nodes.1 = rabbit@gaara-rabbitmq

# إعدادات السجلات
log.console = true
log.console.level = info
log.file = /var/log/rabbitmq/rabbit.log
log.file.level = info
log.file.rotation.date = $D0
log.file.rotation.size = 10485760

# إعدادات الإدارة
management.load_definitions = /etc/rabbitmq/definitions.json
management.rates_mode = basic

# إعدادات الأداء المتقدم
channel_max = 2047
frame_max = 131072
heartbeat = 60
collect_statistics_interval = 5000

# إعدادات الأمان المتقدم
ssl_options.verify = verify_peer
ssl_options.fail_if_no_peer_cert = false
ssl_options.cacertfile = /etc/ssl/certs/ca-certificates.crt

# إعدادات التخزين المؤقت
backing_queue_module = rabbit_variable_queue
msg_store_file_size_limit = 16777216
queue_index_embed_msgs_below = 4096

# إعدادات الشبكة المتقدمة
tcp_listen_options.backlog = 128
tcp_listen_options.nodelay = true
tcp_listen_options.linger.on = true
tcp_listen_options.linger.timeout = 0
tcp_listen_options.exit_on_close = false

# إعدادات المراقبة
management.cors.allow_origins.1 = *
management.cors.max_age = 1800
prometheus.tcp.port = 15692

