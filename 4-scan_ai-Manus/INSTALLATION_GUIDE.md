# دليل التثبيت والإعداد - نظام Gaara Scan AI

## 🚀 التثبيت السريع

### المتطلبات الأساسية
- Docker 20.10+ مع Docker Compose
- ذاكرة وصول عشوائي: 8 جيجابايت على الأقل (16 جيجابايت مُوصى)
- مساحة تخزين: 50 جيجابايت على الأقل
- معالج: 4 أنوية على الأقل
- نظام التشغيل: Linux/Windows/macOS
- GPU (اختياري): NVIDIA مع دعم CUDA

### خطوات التثبيت

#### 1. تحميل وفك ضغط النظام
```bash
# فك ضغط الملف
tar -xzf gaara_scan_ai_complete_with_frontend_integration.tar.gz
cd clean_project
```

#### 2. تشغيل سكريبت التثبيت
```bash
# تشغيل سكريبت التثبيت الشامل
./scripts/install.sh
```

#### 3. الوصول لصفحة الإعداد
بعد اكتمال التثبيت، افتح المتصفح وانتقل إلى:
```
http://localhost/setup
```

## 📋 خطوات الإعداد الأولي

### الخطوة 1: الترحيب
- مراجعة ميزات النظام
- فهم خطوات الإعداد

### الخطوة 2: إنشاء حساب المدير
- اسم المستخدم
- كلمة المرور (8 أحرف على الأقل)
- البريد الإلكتروني (اختياري)

### الخطوة 3: تكوين قاعدة البيانات
- فحص الاتصال بـ PostgreSQL
- تأكيد إعدادات قاعدة البيانات

### الخطوة 4: تكوين الخدمات
- فحص حالة جميع الخدمات
- تفعيل/إلغاء تفعيل الخدمات المطلوبة

### الخطوة 5: إعدادات الأمان
- تكوين SSL (اختياري)
- إعدادات كلمة المرور
- مهلة الجلسة

### الخطوة 6: إكمال الإعداد
- مراجعة الإعدادات
- بدء استخدام النظام

## 🔧 الأوامر المفيدة

### إدارة النظام
```bash
# بدء النظام
./start.sh

# إيقاف النظام
./stop.sh

# إعادة تشغيل النظام
./restart.sh

# فحص حالة النظام
./status.sh

# إنشاء نسخة احتياطية
./backup.sh

# تحديث النظام
./update.sh
```

### إدارة Docker
```bash
# عرض حالة الحاويات
docker-compose ps

# عرض سجلات خدمة معينة
docker-compose logs gaara-main-app

# إعادة بناء خدمة معينة
docker-compose build --no-cache gaara-main-app

# إعادة تشغيل خدمة معينة
docker-compose restart gaara-main-app
```

## 🌐 الروابط المهمة

### الواجهات الرئيسية
- **الصفحة الرئيسية**: http://localhost
- **صفحة الإعداد**: http://localhost/setup
- **لوحة التحكم**: http://localhost/dashboard

### خدمات المراقبة
- **Grafana**: http://localhost:3000
- **Prometheus**: http://localhost:9090
- **Kibana**: http://localhost:5601
- **Portainer**: http://localhost:9000

### خدمات الذكاء الاصطناعي
- **YOLO Detection**: http://localhost:8018
- **Image Enhancement**: http://localhost:8019
- **Plant Hybridization**: http://localhost:8022
- **Plant Disease**: http://localhost:8021

## 📁 هيكل المجلدات

```
clean_project/
├── data/                    # بيانات النظام
│   ├── postgres/           # بيانات قاعدة البيانات
│   ├── yolo/              # بيانات YOLO
│   ├── image_enhancement/ # بيانات تحسين الصور
│   └── ...
├── config/                 # ملفات التكوين
├── logs/                   # ملفات السجلات
├── scripts/               # سكريبتات الإدارة
├── docker/                # ملفات Docker
└── src/                   # الكود المصدري
```

## 🔒 الأمان

### كلمات المرور الافتراضية
يتم توليد كلمات مرور عشوائية آمنة تلقائياً في ملف `.env`

### إعدادات الأمان المُوصى بها
- استخدام كلمات مرور قوية
- تفعيل SSL في البيئة الإنتاجية
- تحديث النظام بانتظام
- مراقبة السجلات

## 🐛 استكشاف الأخطاء

### مشاكل شائعة

#### الخدمات لا تبدأ
```bash
# فحص السجلات
docker-compose logs

# إعادة بناء الحاويات
docker-compose build --no-cache
docker-compose up -d
```

#### نفاد مساحة القرص
```bash
# تنظيف Docker
docker system prune -a

# حذف الصور غير المستخدمة
docker image prune -a
```

#### مشاكل الذاكرة
```bash
# فحص استخدام الذاكرة
docker stats

# إعادة تشغيل الخدمات المستهلكة للذاكرة
docker-compose restart gaara-yolo-detection
```

## 📞 الدعم

### ملفات السجلات
- سجلات التثبيت: `logs/setup_YYYYMMDD_HHMMSS.log`
- سجلات النظام: `logs/system.log`
- سجلات الخدمات: `docker-compose logs [service_name]`

### معلومات النظام
```bash
# معلومات Docker
docker version
docker-compose version

# معلومات النظام
uname -a
free -h
df -h
```

## 🔄 التحديثات

### تحديث النظام
```bash
# تحديث تلقائي
./update.sh

# تحديث يدوي
git pull origin main
docker-compose pull
docker-compose build --no-cache
docker-compose up -d
```

### النسخ الاحتياطية
```bash
# إنشاء نسخة احتياطية
./backup.sh

# استعادة من نسخة احتياطية
tar -xzf backups/backup_YYYYMMDD_HHMMSS.tar.gz
# نسخ الملفات المطلوبة
```

## 🎯 الخطوات التالية

بعد إكمال الإعداد:

1. **استكشف الواجهات**: تصفح جميع الواجهات المتاحة
2. **اختبر الخدمات**: جرب تحميل صور للتشخيص
3. **راجع المراقبة**: تحقق من Grafana و Prometheus
4. **اقرأ التوثيق**: راجع الأدلة المفصلة لكل خدمة
5. **أنشئ نسخة احتياطية**: احفظ إعداداتك

---

**ملاحظة**: هذا النظام مصمم للاستخدام المحلي والتطوير. للاستخدام الإنتاجي، يُرجى مراجعة إعدادات الأمان والأداء.

