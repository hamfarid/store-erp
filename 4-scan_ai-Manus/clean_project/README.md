# نظام Gaara Scan AI - نظام ذكي لتشخيص الأمراض النباتية

## نظرة عامة

نظام Gaara Scan AI هو نظام ذكي متطور لتشخيص الأمراض النباتية والآفات الزراعية باستخدام تقنيات الذكاء الاصطناعي والتعلم الآلي. يوفر النظام حلولاً شاملة للمزارعين والمهندسين الزراعيين لتحديد وتشخيص الأمراض النباتية بدقة عالية.

## الميزات الرئيسية

### 🔍 تشخيص الأمراض النباتية
- تشخيص تلقائي للأمراض النباتية من خلال تحليل الصور
- دعم أكثر من 100 نوع من الأمراض النباتية الشائعة
- دقة تشخيص تصل إلى 95% باستخدام نماذج التعلم العميق

### 🌱 إدارة المحاصيل
- متابعة حالة المحاصيل وصحتها
- توصيات العلاج والوقاية
- تتبع تاريخ الإصابات والعلاجات

### 📊 تحليل البيانات والتقارير
- تقارير مفصلة عن حالة المحاصيل
- إحصائيات الأمراض والآفات
- تحليل الاتجاهات والتنبؤات

### 🔬 البحث عن الصور
- البحث الذكي عن صور الأمراض والآفات
- مكتبة شاملة من الصور المرجعية
- تحديث مستمر لقاعدة البيانات

### 🌐 واجهة ويب متقدمة
- واجهة مستخدم حديثة وسهلة الاستخدام
- دعم اللغة العربية والإنجليزية
- تصميم متجاوب يعمل على جميع الأجهزة

## هيكل المشروع

```
gaara-scan-ai/
├── docs/                           # التوثيق الموحد
│   ├── guides/                     # أدلة المستخدم والمطور
│   │   ├── docker_guide_unified.md
│   │   ├── user_guide_unified.md
│   │   └── developer_guide_unified.md
│   └── references/                 # مستندات مرجعية
├── scripts/                        # سكريبتات التشغيل الموحدة
│   └── start_unified.ps1
├── src/                            # كود المصدر
│   ├── modules/                    # وحدات النظام
│   │   ├── ai_management/          # إدارة الذكاء الاصطناعي
│   │   ├── disease_diagnosis/      # تشخيص الأمراض
│   │   ├── image_search/           # البحث عن الصور
│   │   └── internal_diagnosis/     # التشخيص الداخلي
│   ├── web_interface/              # واجهة الويب
│   │   ├── admin_panel/            # لوحة الإدارة
│   │   └── frontend/               # الواجهة الأمامية
│   ├── services/                   # الخدمات
│   ├── database.py                 # إدارة قاعدة البيانات
│   ├── ai_service.py              # خدمة الذكاء الاصطناعي
│   └── main.py                     # الملف الرئيسي
├── backup_files/                   # نسخ احتياطية للمراجعة
├── docker-compose.yml              # تكوين Docker Compose
├── Dockerfile                      # تعريف صورة Docker
└── README.md                       # هذا الملف
```

## متطلبات النظام

### المتطلبات الأساسية
- Python 3.8 أو أحدث
- Node.js 16 أو أحدث
- Docker و Docker Compose (للنشر)
- PostgreSQL أو MySQL (قاعدة البيانات)

### المكتبات المطلوبة
- FastAPI (إطار عمل الواجهة الخلفية)
- React (الواجهة الأمامية)
- TensorFlow/PyTorch (نماذج التعلم الآلي)
- OpenCV (معالجة الصور)
- SQLAlchemy (ORM)

## التثبيت والإعداد

### 1. استنساخ المشروع

```bash
git clone https://github.com/your-repo/gaara-scan-ai.git
cd gaara-scan-ai
```

### 2. إعداد البيئة الافتراضية

```bash
python -m venv venv
source venv/bin/activate  # على Windows: venv\Scripts\activate
```

### 3. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 4. إعداد متغيرات البيئة

انسخ ملف `.env.example` إلى `.env` وقم بتحديث القيم:

```bash
cp .env.example .env
```

### 5. تشغيل النظام

#### باستخدام Docker (الطريقة المفضلة)

```bash
docker-compose build
docker-compose up -d
```

#### تشغيل محلي

```bash
# تشغيل الخادم الخلفي
python src/main.py

# في نافذة طرفية أخرى، تشغيل الواجهة الأمامية
cd src/web_interface/admin_panel
npm install
npm start
```

## الاستخدام

### الوصول إلى النظام

بعد تشغيل النظام، يمكنك الوصول إليه عبر:

- **الواجهة الرئيسية**: http://localhost:3000
- **لوحة الإدارة**: http://localhost:3000/admin
- **واجهة برمجة التطبيقات**: http://localhost:8000/docs

### الخطوات الأساسية

1. **تسجيل الدخول**: استخدم بيانات المدير الافتراضية
2. **رفع صورة**: ارفع صورة النبات المصاب
3. **التشخيص**: انتظر نتائج التشخيص التلقائي
4. **مراجعة النتائج**: اطلع على التشخيص والتوصيات
5. **حفظ السجل**: احفظ النتائج في قاعدة البيانات

## الوحدات الرئيسية

### 1. وحدة تشخيص الأمراض
- تحليل الصور باستخدام الذكاء الاصطناعي
- تحديد نوع المرض ودرجة الإصابة
- تقديم توصيات العلاج

### 2. وحدة البحث عن الصور
- البحث في قواعد البيانات المحلية والخارجية
- جمع وتصنيف الصور المرجعية
- تحديث مستمر للمحتوى

### 3. وحدة إدارة الذكاء الاصطناعي
- إدارة نماذج التعلم الآلي
- تدريب وتحديث النماذج
- مراقبة الأداء والدقة

### 4. واجهة الإدارة
- إدارة المستخدمين والصلاحيات
- مراقبة النظام والإحصائيات
- إعدادات النظام والتكوين

## التطوير والمساهمة

### إعداد بيئة التطوير

1. اتبع خطوات التثبيت أعلاه
2. قم بتثبيت أدوات التطوير الإضافية:

```bash
pip install -r requirements-dev.txt
```

3. قم بتشغيل الاختبارات:

```bash
pytest tests/
```

### إرشادات المساهمة

1. قم بعمل Fork للمشروع
2. أنشئ فرعاً جديداً للميزة (`git checkout -b feature/amazing-feature`)
3. اكتب الكود واختبره
4. قم بعمل Commit للتغييرات (`git commit -m 'Add amazing feature'`)
5. ادفع الفرع (`git push origin feature/amazing-feature`)
6. افتح Pull Request

## الدعم والمساعدة

### التوثيق المفصل

- [دليل المستخدم الشامل](docs/guides/user_guide_unified.md)
- [دليل المطور](docs/guides/developer_guide_unified.md)
- [دليل Docker](docs/guides/docker_guide_unified.md)

### الحصول على المساعدة

- **المشاكل التقنية**: افتح Issue في GitHub
- **الأسئلة العامة**: راجع التوثيق أو اتصل بفريق الدعم
- **طلبات الميزات**: اقترح ميزات جديدة عبر GitHub

## الترخيص

هذا المشروع مرخص تحت رخصة MIT. راجع ملف [LICENSE](LICENSE) للتفاصيل.

## الشكر والتقدير

نشكر جميع المساهمين في تطوير هذا النظام والمجتمع المفتوح المصدر على الدعم المستمر.

---

**ملاحظة**: هذا النظام في مرحلة التطوير المستمر. نرحب بالملاحظات والاقتراحات لتحسين الأداء والوظائف.

