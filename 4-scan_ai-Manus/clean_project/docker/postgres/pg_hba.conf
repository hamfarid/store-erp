# إعدادات المصادقة لقاعدة البيانات
# الملف: /home/ubuntu/clean_project/docker/postgres/pg_hba.conf

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             0.0.0.0/0               scram-sha-256

# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256

# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            scram-sha-256
host    replication     all             ::1/128                 scram-sha-256

# Docker network connections
host    all             all             172.20.0.0/16           scram-sha-256
host    all             all             172.21.0.0/16           scram-sha-256
host    all             all             172.22.0.0/16           scram-sha-256
host    all             all             172.23.0.0/16           scram-sha-256

# Specific database connections
host    gaara_scan_ai   gaara_user      0.0.0.0/0               scram-sha-256
host    grafana         grafana_user    0.0.0.0/0               scram-sha-256
host    gaara_memory    memory_user     0.0.0.0/0               scram-sha-256
host    gaara_vector    vector_user     0.0.0.0/0               scram-sha-256
host    gaara_analytics analytics_user  0.0.0.0/0               scram-sha-256

