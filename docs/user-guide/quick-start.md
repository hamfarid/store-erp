# دليل البدء السريع

## مرحباً بك في Store!

هذا الدليل سيساعدك على البدء مع نظام إدارة المخزون العربي بسرعة وسهولة.

## المتطلبات الأساسية

قبل البدء، تأكد من توفر المتطلبات التالية على نظامك:

- **Python 3.8+** - لتشغيل الواجهة الخلفية
- **Node.js 16+** - لتشغيل الواجهة الأمامية
- **npm أو yarn** - لإدارة حزم JavaScript
- **Git** - لاستنساخ المستودع

## التثبيت

### 1. استنساخ المستودع

```bash
git clone https://github.com/hamfarid/Store.git
cd Store
```

### 2. التشغيل السريع

أسهل طريقة لتشغيل النظام هي استخدام السكريبت المُعد مسبقاً:

```bash
chmod +x scripts/start_system.sh
./scripts/start_system.sh
```

هذا السكريبت سيقوم بـ:
- تثبيت جميع التبعيات المطلوبة
- إعداد قاعدة البيانات
- تشغيل الواجهة الخلفية والأمامية

### 3. التشغيل المنفصل

إذا كنت تفضل تشغيل المكونات بشكل منفصل:

#### الواجهة الخلفية (Backend)

```bash
cd backend
pip install -r requirements.txt
python app.py
```

#### الواجهة الأمامية (Frontend)

```bash
cd frontend
npm install
npm run dev
```

## الوصول للنظام

بعد التشغيل الناجح، يمكنك الوصول إلى:

- **الواجهة الأمامية**: [http://localhost:3000](http://localhost:3000)
- **API الخلفية**: [http://localhost:5001](http://localhost:5001)
- **توثيق API**: [http://localhost:5001/api/docs](http://localhost:5001/api/docs)

## تسجيل الدخول الأول

### بيانات المدير الافتراضية

- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`

::: warning تحذير أمني
يرجى تغيير كلمة المرور الافتراضية فور تسجيل الدخول الأول لضمان أمان النظام.
:::

## الخطوات الأولى

### 1. تغيير كلمة المرور

1. سجل دخولك باستخدام البيانات الافتراضية
2. اذهب إلى **الإعدادات** → **الملف الشخصي**
3. اختر **تغيير كلمة المرور**
4. أدخل كلمة مرور قوية جديدة

### 2. إعداد معلومات الشركة

1. اذهب إلى **الإعدادات** → **معلومات الشركة**
2. أدخل:
   - اسم الشركة
   - العنوان
   - رقم الهاتف
   - البريد الإلكتروني
   - الشعار (اختياري)

### 3. إضافة أول منتج

1. اذهب إلى **المنتجات** → **إضافة منتج جديد**
2. أدخل المعلومات الأساسية:
   - اسم المنتج
   - الكود (SKU)
   - الفئة
   - السعر
   - الكمية الأولية

### 4. إضافة أول عميل

1. اذهب إلى **العملاء** → **إضافة عميل جديد**
2. أدخل بيانات العميل:
   - الاسم
   - رقم الهاتف
   - العنوان
   - البريد الإلكتروني (اختياري)

### 5. إنشاء أول فاتورة

1. اذهب إلى **المبيعات** → **فاتورة جديدة**
2. اختر العميل
3. أضف المنتجات المباعة
4. احفظ الفاتورة

## نصائح مهمة

### الأمان
- استخدم كلمات مرور قوية
- قم بعمل نسخ احتياطية دورية
- لا تشارك بيانات الدخول

### الأداء
- نظف البيانات القديمة دورياً
- راقب مساحة القرص الصلب
- استخدم فلاتر البحث لتسريع العمليات

### النسخ الاحتياطي
- قم بعمل نسخة احتياطية يومية من قاعدة البيانات
- احفظ النسخ في مكان آمن
- اختبر استعادة النسخ الاحتياطية دورياً

## المشاكل الشائعة

### لا يمكن الوصول للنظام

**الحل**:
1. تأكد من تشغيل الخوادم
2. تحقق من المنافذ (3000 للواجهة، 5001 للخلفية)
3. تأكد من عدم وجود برامج أخرى تستخدم نفس المنافذ

### رسالة خطأ في قاعدة البيانات

**الحل**:
1. تأكد من وجود ملف قاعدة البيانات
2. تحقق من صلاحيات الملفات
3. أعد تشغيل النظام

### الواجهة لا تظهر بالعربية

**الحل**:
1. تأكد من إعدادات المتصفح
2. امسح ذاكرة التخزين المؤقت
3. أعد تحميل الصفحة

## الحصول على المساعدة

إذا واجهت أي مشاكل:

1. **راجع التوثيق**: [التوثيق التقني](/technical/)
2. **ابحث في المشاكل المعروفة**: [GitHub Issues](https://github.com/hamfarid/Store/issues)
3. **أنشئ مشكلة جديدة**: إذا لم تجد حلاً

## الخطوات التالية

بعد إعداد النظام بنجاح، يمكنك:

- [تعلم إدارة المنتجات](./products.md)
- [فهم نظام العملاء](./customers.md)
- [استكشاف ميزات المبيعات](./sales.md)
- [مراجعة التقارير المتاحة](./reports.md)

---

مبروك! أنت الآن جاهز لاستخدام نظام Store لإدارة مخزونك بكفاءة. 🎉
