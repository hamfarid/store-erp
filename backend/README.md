# نظام إدارة المخزون

<!-- Replace OWNER/REPO below with your GitHub org/repo to enable the badge -->
[![Backend Quickcheck](https://github.com/OWNER/REPO/actions/workflows/python-quickcheck.yml/badge.svg)](https://github.com/OWNER/REPO/actions/workflows/python-quickcheck.yml)

نظام ويب شامل لإدارة المخزون مع قواعد بيانات وشاشات إدخال وطباعة تقارير ونظام صلاحيات متقدم.

## المميزات

### 🏢 إدارة المخزون
- إدارة الأصناف والمنتجات
- تصنيفات ومجموعات المنتجات
- إدارة المخازن والمواقع
- تتبع الكميات والأسعار
- إدارة الدفعات وتواريخ الانتهاء

### 💰 المبيعات والمشتريات
- فواتير المبيعات والمشتريات
- إدارة العملاء والموردين
- تتبع المدفوعات والمستحقات
- أسعار صرف متعددة العملات

### 📊 التقارير والتحليلات
- تقارير المخزون الحالي
- تقارير الحركات والمعاملات
- تقارير المبيعات والمشتريات
- تقارير مالية شاملة
- إمكانية الطباعة والتصدير (PDF/Excel)

### 👥 إدارة المستخدمين
- نظام تسجيل دخول آمن
- أدوار وصلاحيات متعددة
- تتبع نشاطات المستخدمين
- إدارة كلمات المرور

### 📁 استيراد البيانات
- استيراد من ملفات Excel
- قوالب جاهزة للاستيراد
- معالجة البيانات تلقائياً
- تحليل وتنظيف البيانات

## التقنيات المستخدمة

### الواجهة الخلفية (Backend)
- **Python 3.11**
- **Flask** - إطار عمل الويب
- **SQLAlchemy** - ORM لقاعدة البيانات
- **SQLite** - قاعدة البيانات
- **Pandas** - معالجة البيانات
- **ReportLab** - إنشاء ملفات PDF

### الواجهة الأمامية (Frontend)
- **HTML5** - هيكل الصفحات
- **CSS3** - التصميم والتنسيق
- **JavaScript** - التفاعل والديناميكية
- **Font Awesome** - الأيقونات
- **Responsive Design** - متوافق مع الأجهزة المحمولة

## متطلبات النظام

- Python 3.11 أو أحدث
- متصفح ويب حديث (Chrome, Firefox, Safari, Edge)
- 512 MB RAM كحد أدنى
- 100 MB مساحة تخزين

## التثبيت والتشغيل

### 1. تحميل المشروع
```bash
git clone <repository-url>
cd inventory_management_system
```

### 2. إنشاء البيئة الافتراضية
```bash
python -m venv venv
```

### 3. تفعيل البيئة الافتراضية

**على Windows:**
```bash
venv\Scripts\activate
```

**على Linux/Mac:**
```bash
source venv/bin/activate
```

### 4. تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 5. تشغيل النظام
```bash
cd src
python main.py
```

### 6. فتح النظام في المتصفح
افتح المتصفح وانتقل إلى: `http://localhost:5001`

## بيانات تسجيل الدخول الافتراضية

- **اسم المستخدم**: admin
- **كلمة المرور**: admin123

> **تحذير**: يُنصح بشدة بتغيير كلمة المرور الافتراضية فور تسجيل الدخول لأول مرة.

## هيكل المشروع

```
inventory_management_system/
├── src/
│   ├── main.py                 # الملف الرئيسي للتطبيق
│   ├── models/                 # نماذج قاعدة البيانات
│   │   ├── user.py            # نموذج المستخدمين والأدوار
│   │   ├── inventory.py       # نماذج المخزون
│   │   └── partners.py        # نماذج العملاء والموردين
│   ├── routes/                 # مسارات API
│   │   ├── user.py            # APIs المستخدمين
│   │   ├── inventory.py       # APIs المخزون
│   │   ├── partners.py        # APIs العملاء والموردين
│   │   ├── reports.py         # APIs التقارير
│   │   ├── admin.py           # APIs الإدارة
│   │   └── import_data.py     # APIs استيراد البيانات
│   ├── static/                 # الملفات الثابتة
│   │   ├── index.html         # الواجهة الرئيسية
│   │   ├── reports.html       # واجهة التقارير
│   │   ├── admin.html         # واجهة الإدارة
│   │   └── import.html        # واجهة استيراد البيانات
│   └── database/               # ملفات قاعدة البيانات
├── venv/                       # البيئة الافتراضية
├── user_manual.md             # دليل المستخدم
├── requirements.txt           # متطلبات Python
└── README.md                  # هذا الملف
```

## الاستخدام السريع

### 1. إضافة صنف جديد
1. سجل دخولك إلى النظام
2. اضغط على "إدارة المخزون"
3. اختر "إضافة صنف جديد"
4. املأ البيانات المطلوبة
5. اضغط "حفظ"

### 2. إنشاء فاتورة مبيعات
1. اضغط على "المبيعات"
2. اختر "فاتورة جديدة"
3. اختر العميل
4. أضف الأصناف والكميات
5. احفظ الفاتورة

### 3. إنشاء تقرير
1. اضغط على "التقارير"
2. اختر نوع التقرير
3. حدد المعايير والفترة
4. اضغط "إنشاء التقرير"

### 4. استيراد بيانات من Excel
1. اضغط على "استيراد من Excel"
2. حمل القالب المناسب
3. املأ القالب ببياناتك
4. ارفع الملف
5. راجع البيانات واضغط "استيراد"

## الأمان

### كلمات المرور
- استخدم كلمات مرور قوية (8 أحرف على الأقل)
- امزج بين الأحرف والأرقام والرموز
- لا تشارك كلمات المرور مع الآخرين
- غيّر كلمة المرور بانتظام

### النسخ الاحتياطي
- قم بعمل نسخة احتياطية من قاعدة البيانات يومياً
- احتفظ بالنسخ الاحتياطية في مكان آمن
- اختبر استعادة النسخ الاحتياطية بانتظام

### الصلاحيات
- امنح المستخدمين أقل الصلاحيات اللازمة لعملهم
- راجع صلاحيات المستخدمين بانتظام
- احذف حسابات المستخدمين غير النشطين

## استكشاف الأخطاء

### مشاكل شائعة وحلولها

#### النظام لا يعمل
```bash
# تأكد من تفعيل البيئة الافتراضية
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# تأكد من تثبيت المتطلبات
pip install -r requirements.txt

# تشغيل النظام
cd src
python main.py
```

#### خطأ في قاعدة البيانات
```bash
# احذف قاعدة البيانات الحالية وأعد إنشاءها
rm src/database/app.db
python src/main.py
```

#### مشاكل في استيراد البيانات
- تأكد من استخدام القالب الصحيح
- تحقق من صيغة الملف (.xlsx أو .xls)
- تأكد من عدم وجود خلايا فارغة في الأعمدة المطلوبة

## التطوير والتخصيص

### إضافة ميزات جديدة
1. أنشئ نموذج جديد في مجلد `models/`
2. أضف APIs في مجلد `routes/`
3. أنشئ واجهة في مجلد `static/`
4. سجل الـ blueprint في `main.py`

### تخصيص التصميم
- عدّل ملفات CSS في الواجهات
- أضف أيقونات جديدة من Font Awesome
- غيّر الألوان والخطوط حسب هوية شركتك

## الدعم والمساعدة

### الموارد
- **دليل المستخدم**: `user_manual.md`
- **التوثيق الفني**: في التعليقات داخل الكود
- **أمثلة الاستخدام**: في مجلد `examples/`

### الإبلاغ عن المشاكل
إذا واجهت أي مشاكل أو لديك اقتراحات للتحسين، يرجى:
1. التأكد من اتباع تعليمات التثبيت بدقة
2. البحث في المشاكل الشائعة أعلاه
3. التواصل مع فريق الدعم الفني

## الترخيص

هذا المشروع مرخص تحت رخصة MIT. راجع ملف `LICENSE` للمزيد من التفاصيل.

## المساهمة

نرحب بمساهماتكم في تطوير النظام! يرجى:
1. عمل Fork للمشروع
2. إنشاء فرع جديد للميزة
3. إضافة التحسينات
4. إرسال Pull Request

## الإصدارات

### الإصدار 1.0 (يونيو 2025)
- الإصدار الأولي
- جميع الميزات الأساسية
- واجهة مستخدم كاملة
- نظام التقارير
- استيراد البيانات

### خطط المستقبل
- تطبيق محمول
- تكامل مع أنظمة المحاسبة
- تقارير متقدمة بالذكاء الاصطناعي
- دعم قواعد بيانات متعددة

---

**شكراً لاستخدام نظام إدارة المخزون!**

للمزيد من المعلومات، راجع دليل المستخدم أو تواصل مع فريق الدعم الفني.

