# 🚀 دليل التشغيل السريع - نظام إدارة المتجر v1.5
## Quick Start Guide - Store Management System v1.5

---

## 📋 **متطلبات النظام**

### الحد الأدنى:
- **Python:** 3.8+ 
- **Node.js:** 18+
- **RAM:** 2GB
- **مساحة القرص:** 2GB
- **نظام التشغيل:** Windows 10+, macOS 10.15+, Ubuntu 18.04+

### المُوصى به:
- **Python:** 3.11+
- **Node.js:** 20+
- **RAM:** 4GB+
- **مساحة القرص:** 5GB+

---

## ⚡ **التشغيل السريع**

### 🐧 Linux/macOS:
```bash
# تثبيت كامل وتشغيل
./run_complete_system.sh install
./run_complete_system.sh start

# أو خطوة بخطوة
./run_complete_system.sh check    # فحص المتطلبات
./run_complete_system.sh install  # تثبيت كامل
./run_complete_system.sh start    # تشغيل النظام
```

### 🪟 Windows:
```cmd
# تشغيل سكريبت الإعداد
run_system.bat

# أو يدوياً
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py

# في نافذة أخرى
cd frontend
npm install
npm run dev
```

---

## 🔧 **الأوامر المتاحة**

| الأمر | الوصف |
|-------|--------|
| `install` | تثبيت كامل للنظام |
| `start` | تشغيل النظام |
| `stop` | إيقاف النظام |
| `restart` | إعادة تشغيل |
| `status` | فحص الحالة |
| `build` | بناء الواجهة الأمامية |
| `setup` | إعداد قاعدة البيانات |
| `security` | تطبيق الأمان |
| `check` | فحص المتطلبات |

---

## 🌐 **الروابط المهمة**

- **🏠 الصفحة الرئيسية:** http://localhost:5502
- **🔧 API الخلفية:** http://localhost:5001
- **📊 فحص الصحة:** http://localhost:5001/api/health
- **📚 توثيق API:** http://localhost:5001/docs

---

## 👑 **معلومات تسجيل الدخول**

### مستخدم Admin الافتراضي:
- **اسم المستخدم:** `admin`
- **كلمة المرور:** موجودة في `admin_credentials.json`
- **البريد الإلكتروني:** `admin@store-system.com`

⚠️ **مهم:** غيّر كلمة المرور فور تسجيل الدخول الأول!

---

## 🛠️ **استكشاف الأخطاء**

### مشاكل شائعة:

#### 1. خطأ في المنفذ:
```bash
# إيقاف العمليات المتعارضة
pkill -f "python.*app.py"
pkill -f "npm.*run.*dev"
```

#### 2. مشاكل قاعدة البيانات:
```bash
cd backend
rm -rf instance/store.db
python -c "from src.database import create_app, db; app = create_app(); app.app_context().push(); db.create_all()"
```

#### 3. مشاكل Node.js:
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

#### 4. مشاكل Python:
```bash
cd backend
rm -rf venv
python -m venv venv
source venv/bin/activate  # Linux/macOS
# أو venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

---

## 📁 **هيكل المشروع**

```
store_v1.5/
├── backend/                 # الواجهة الخلفية (Flask)
│   ├── src/                # الكود المصدري
│   ├── instance/           # قاعدة البيانات
│   ├── venv/              # البيئة الافتراضية
│   └── requirements.txt    # متطلبات Python
├── frontend/               # الواجهة الأمامية (React)
│   ├── src/               # الكود المصدري
│   ├── dist/              # الملفات المبنية
│   └── package.json       # متطلبات Node.js
├── logs/                  # سجلات النظام
├── admin_credentials.json # معلومات Admin
└── run_complete_system.sh # سكريبت التشغيل
```

---

## 🔒 **الأمان**

### الميزات الأمنية:
- ✅ تشفير كلمات المرور (bcrypt)
- ✅ JWT للمصادقة
- ✅ CORS محدود
- ✅ Rate Limiting
- ✅ SQL Injection Protection
- ✅ XSS Protection

### تحسين الأمان:
```bash
# تطبيق الأمان العسكري
./run_complete_system.sh security

# أو يدوياً
python military_grade_security.py
./security_hardening.sh
```

---

## 📊 **المراقبة**

### فحص الحالة:
```bash
# فحص شامل
./run_complete_system.sh status

# فحص API
curl http://localhost:5001/api/health

# فحص العمليات
ps aux | grep -E "(python.*app.py|npm.*run.*dev)"
```

### السجلات:
```bash
# سجل الواجهة الخلفية
tail -f logs/backend.log

# سجل الواجهة الأمامية
tail -f logs/frontend.log
```

---

## 🚀 **النشر للإنتاج**

### إعداد الإنتاج:
```bash
# بناء الواجهة الأمامية
cd frontend
npm run build

# تشغيل الخادم بـ Gunicorn
cd backend
source venv/bin/activate
gunicorn -w 4 -b 0.0.0.0:5001 app:app
```

### متغيرات البيئة للإنتاج:
```bash
export FLASK_ENV=production
export SECRET_KEY="your-secret-key"
export DATABASE_URL="your-database-url"
```

---

## 📞 **الدعم**

### في حالة المشاكل:
1. تحقق من السجلات في مجلد `logs/`
2. تأكد من تشغيل جميع الخدمات
3. فحص متطلبات النظام
4. إعادة تشغيل النظام

### معلومات النظام:
- **الإصدار:** v1.5
- **تاريخ الإصدار:** 2025-10-04
- **مستوى الأمان:** Military Grade
- **الترخيص:** MIT

---

## 🎉 **مبروك!**

النظام جاهز للاستخدام! استمتع بإدارة متجرك بكفاءة وأمان عاليين.

**🌟 نصائح للنجاح:**
- غيّر كلمة مرور Admin فوراً
- اعمل نسخ احتياطية دورية
- راقب السجلات بانتظام
- حدّث النظام دورياً
