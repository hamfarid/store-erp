# 🚀 Autonomous Multi-Agent System v2.0.0 - دليل النظام المتكامل

**Version:** 2.0.0  
**Date:** November 5, 2025  
**Author:** Manus AI

---

## 📖 جدول المحتويات

1. [مقدمة: ما هو النظام المتكامل؟](#1-مقدمة-ما-هو-النظام-المتكامل)
2. [المعمارية الموحدة (v2.0.0)](#2-المعمارية-الموحدة-v200)
3. [مكونات النظام](#3-مكونات-النظام)
4. [التثبيت والإعداد (خطوة بخطوة)](#4-التثبيت-والإعداد-خطوة-بخطوة)
5. [الاستخدام و Workflows](#5-الاستخدام-و-workflows)
6. [التكوين المتقدم](#6-التكوين-المتقدم)
7. [استكشاف الأخطاء وإصلاحها](#7-استكشاف-الأخطاء-وإصلاحها)

---

## 1. مقدمة: ما هو النظام المتكامل؟

هذا الإصدار (v2.0.0) يمثل نقلة نوعية من مجرد نظام متعدد الوكلاء إلى **منصة تطوير برمجيات متكاملة وتلقائية بالكامل**. قمنا بدمج أدوات رائدة في الصناعة لإنشاء workflow سلس يغطي دورة حياة التطوير بأكملها، من الفكرة إلى النشر.

### ✨ الجديد في v2.0.0

- **واجهة `Continue`**: استبدلنا `Augment` بـ `Continue.dev` كواجهة أساسية، مما يوفر تكاملاً أعمق مع VS Code/JetBrains، ودعم CLI، ومرونة لا مثيل لها.
- **مراجعة كود تلقائية مع `CodeRabbit`**: تم دمج `CodeRabbit` لمراجعة كل Pull Request تلقائياً، مما يضيف طبقة قوية من ضمان الجودة والأمان.
- **CI/CD متكامل**: تم إنشاء GitHub Actions workflow لأتمتة المراجعة، الاختبار، والنشر.
- **بنية Docker متكاملة**: الآن يمكنك تشغيل النظام بأكمله (Orchestrator, MCP, Webhook Handler) باستخدام `docker-compose`.
- **Auto-Fix Workflow**: النظام الآن قادر على إصلاح الأخطاء التي يكتشفها `CodeRabbit` تلقائياً.

### 🎯 الهدف

الهدف هو تمكين المطورين من التركيز على **ماذا** يريدون بناءه، بينما يتولى النظام **كيفية** بنائه، مراجعته، اختباره، ونشره بأعلى جودة ممكنة.

---

## 2. المعمارية الموحدة (v2.0.0)

تعتمد المعمارية الجديدة على 5 طبقات متكاملة تعمل معاً بسلاسة.

```mermaid
graph TD
    subgraph Layer 1: Interface
        A[Continue.dev <br>(VS Code/CLI)]
    end

    subgraph Layer 2: Communication
        B[MCP Server <br>(autonomous-builder)]
    end

    subgraph Layer 3: Orchestration
        C[Autonomous Orchestrator <br>(Enhanced)]
    end

    subgraph Layer 4: Agents
        D[Lead Agent (Gemini)]
        E[Reviewer Agent (Claude)]
        F[Consultant Agent (ChatGPT)]
    end

    subgraph Layer 5: External Tools
        G[GitHub]
        H[CodeRabbit]
        I[EarlyAI (Optional)]
        J[GitHub Actions]
    end

    A -->|MCP Call| B
    B -->|Trigger| C
    C -->|Coordinate| D
    C -->|Coordinate| E
    C -->|Coordinate| F
    D -->|Code| G
    E -->|Test| G
    G -->|Webhook| H
    H -->|Webhook| C
    G -->|CI/CD| J
    J -->|Run| C
```

| الطبقة | المكون الرئيسي | الوظيفة |
|---|---|---|
| **1. Interface** | `Continue.dev` | الواجهة الأساسية للمطور للتفاعل مع النظام عبر VS Code أو CLI. |
| **2. Communication** | `MCP Server` | جسر التواصل بين الواجهة والـ Orchestrator. |
| **3. Orchestration** | `Autonomous Orchestrator` | العقل المدبر للنظام؛ ينسق بين الوكلاء والأدوات. |
| **4. Agents** | `Gemini`, `Claude`, `ChatGPT` | الوكلاء الذين يقومون بالمهام الفعلية (تصميم، كود، مراجعة). |
| **5. External Tools** | `GitHub`, `CodeRabbit` | أدوات خارجية للتخزين، المراجعة، والأتمتة. |

---

## 3. مكونات النظام

### `Continue.dev` (الواجهة)
- **لماذا؟** لأنه open-source، مرن، ويدعم MCP و CLI natively.
- **كيف؟** نستخدم `continue_config.json` لتعريف الأوامر المخصصة (`/build`, `/review`, `/fix`) التي تستدعي نظامنا.

### `CodeRabbit` (المراجعة التلقائية)
- **لماذا؟** يضيف طبقة مراجعة ذكية تتعلم من codebase وتكتشف أخطاء وثغرات أمنية.
- **كيف؟** يتم تفعيله على GitHub repo. عند إنشاء PR، يقوم بمراجعة الكود. إذا وجد أخطاء، يرسل webhook إلى `coderabbit_webhook.py` الذي بدوره يستدعي الـ Orchestrator لإصلاحها.

### `Autonomous Orchestrator` (العقل المدبر)
- **لماذا؟** هو قلب النظام الذي يدير الـ workflow ويوزع المهام.
- **كيف؟** تم تحسينه ليتفاعل مع webhooks من `CodeRabbit` ويقوم بتشغيل مهام الإصلاح التلقائي.

### `GitHub Actions` (الأتمتة)
- **لماذا؟** لأتمتة عمليات المراجعة والاختبار في CI/CD pipeline.
- **كيف؟** ملف `autonomous-ci.yml` يعرف jobs لتشغيل المراجعة التلقائية وتوليد الاختبارات مع كل PR.

---

## 4. التثبيت والإعداد (خطوة بخطوة)

### المتطلبات الأساسية

1. **Docker & Docker Compose**: [تثبيت Docker](https://docs.docker.com/get-docker/)
2. **VS Code**: [تثبيت VS Code](https://code.visualstudio.com/)
3. **Continue Extension**: [تثبيت من VS Code Marketplace](https://marketplace.visualstudio.com/items?itemName=Continue.continue)
4. **GitHub Account**: حساب GitHub.
5. **CodeRabbit Account**: [حساب CodeRabbit](https://coderabbit.ai/) (الخطة المجانية كافية للبدء).

### الخطوة 1: تحميل وإعداد النظام

```bash
# 1. استنساخ المستودع
git clone https://github.com/hamfarid/global.git
cd global/autonomous-multiagent-system

# 2. إعداد ملف البيئة
cp .env.example .env

# 3. تحرير ملف .env وإضافة مفاتيح API
nano .env
# أضف مفاتيحك:
# GEMINI_API_KEY="..."
# ANTHROPIC_API_KEY="..."
# OPENAI_API_KEY="..."
# GITHUB_TOKEN="..." (مهم جداً)
# CODERABBIT_WEBHOOK_SECRET="..." (أنشئ secret قوي)
```

### الخطوة 2: إعداد Continue في VS Code

1. افتح VS Code.
2. افتح Command Palette (`Ctrl+Shift+P`).
3. ابحث عن `Continue: Open Config File`.
4. **استبدل** محتوى الملف بالكامل بمحتويات ملف `continue_config.json` الموجود في النظام.
5. احفظ الملف. سيقوم Continue بإعادة التحميل تلقائياً.

### الخطوة 3: إعداد CodeRabbit

1. اذهب إلى [CodeRabbit](https://coderabbit.ai/) وسجل الدخول باستخدام حساب GitHub.
2. قم بتثبيت تطبيق CodeRabbit على مستودعك.
3. في إعدادات المستودع على GitHub، اذهب إلى `Webhooks`.
4. أضف webhook جديد:
   - **Payload URL**: `http://<YOUR_PUBLIC_IP>:8000/webhook/coderabbit` (استخدم ngrok أو ما شابه للاختبار المحلي).
   - **Content type**: `application/json`.
   - **Secret**: نفس الـ `CODERABBIT_WEBHOOK_SECRET` الذي وضعته في `.env`.
   - **Events**: اختر `Send me everything` أو اختر `Pull request reviews`.

### الخطوة 4: تشغيل النظام باستخدام Docker Compose

```bash
# من داخل مجلد autonomous-multiagent-system
docker-compose up --build
```

هذا الأمر سيقوم بـ:
- بناء وتشغيل `orchestrator`.
- بناء وتشغيل `mcp-server`.
- بناء وتشغيل `webhook-handler` (الذي يستمع لـ CodeRabbit).

---

## 5. الاستخدام و Workflows

### Workflow 1: التطوير باستخدام Continue (داخل VS Code)

1. افتح مشروعك في VS Code.
2. افتح نافذة Continue (`Ctrl+L` أو من الشريط الجانبي).
3. اكتب في الشات:
   ```
   @build Build a REST API for user authentication with JWT
   ```
4. سيقوم النظام ببناء المشروع كاملاً في مجلد `output`.
5. قم بإنشاء PR بالتغييرات على GitHub.

### Workflow 2: المراجعة التلقائية مع CodeRabbit

1. بمجرد إنشاء PR، سيقوم `CodeRabbit` بمراجعة الكود تلقائياً وإضافة تعليقات.
2. إذا وجد `CodeRabbit` أخطاء قابلة للإصلاح، سيقوم بإرسال webhook.
3. `webhook-handler` سيستقبل الـ webhook ويستدعي الـ `Orchestrator`.
4. الـ `Orchestrator` سيقوم بتشغيل مهمة `fix` ويقوم بإنشاء commit جديد بالإصلاحات ودفعه إلى الـ PR.
5. `CodeRabbit` سيقوم بمراجعة التغييرات الجديدة مرة أخرى.

### Workflow 3: CI/CD مع GitHub Actions

1. قم بنسخ ملف `.github/workflows/autonomous-ci.yml` إلى مستودعك.
2. في إعدادات المستودع على GitHub، اذهب إلى `Secrets and variables` > `Actions`.
3. أضف الـ secrets التالية:
   - `GEMINI_API_KEY`
   - `ANTHROPIC_API_KEY`
   - `OPENAI_API_KEY`
   - `GITHUB_TOKEN` (استخدم `secrets.GITHUB_TOKEN` الافتراضي أو أنشئ واحد جديد)
4. الآن، مع كل PR، ستقوم GitHub Actions بتشغيل:
   - **Autonomous Review**: مراجعة تلقائية باستخدام نظامنا.
   - **Autonomous Test**: توليد وتشغيل اختبارات تلقائية.
   - **Security Scan**: فحص أمني باستخدام CodeQL.

---

## 6. التكوين المتقدم

### تخصيص `continue_config.json`
- **Models**: يمكنك إضافة نماذج لغة أخرى (مثل Ollama محلياً).
- **Slash Commands**: يمكنك تعديل أو إضافة أوامر مخصصة.
- **Context Providers**: يمكنك التحكم في السياق الذي يحصل عليه Continue.

### تخصيص `coderabbit_webhook.py`
- يمكنك تعديل `format_fix_requirement` لتغيير كيفية صياغة متطلبات الإصلاح للـ Orchestrator.
- يمكنك تعديل `extract_issues_from_review` لتصفية أنواع معينة من الأخطاء.

### تخصيص `autonomous-ci.yml`
- يمكنك تفعيل أو تعطيل jobs معينة.
- يمكنك تغيير الـ triggers (متى يتم تشغيل الـ workflow).

---

## 7. استكشاف الأخطاء وإصلاحها

- **مشكلة: Continue لا يتعرف على الأوامر المخصصة.**
  - **الحل:** تأكد من أنك قمت بنسخ `continue_config.json` بشكل صحيح وأن Continue قام بإعادة التحميل. أعد تشغيل VS Code.

- **مشكلة: Webhook لا يصل.**
  - **الحل:** تأكد من أن الـ Payload URL صحيح وأن المنفذ (8000) مفتوح. استخدم `ngrok` للاختبار المحلي. تحقق من أن الـ secret متطابق.

- **مشكلة: Docker Compose يفشل في البناء.**
  - **الحل:** تأكد من أن لديك اتصال بالإنترنت. حاول تشغيل `docker-compose build --no-cache`.

- **مشكلة: GitHub Actions تفشل.**
  - **الحل:** تحقق من أن جميع الـ secrets تم إعدادها بشكل صحيح في إعدادات المستودع. تحقق من logs الخاصة بالـ action لمزيد من التفاصيل.

---

🚀 **Happy Autonomous Coding!** 🚀

