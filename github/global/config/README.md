# Config — Global System v26.0.2 Diamond 32

> Configuration files for development tools and services.

## Files
- `mcp_config.json` — MCP server configuration
- `pyproject.toml` — Python project settings (black, mypy, pytest)
- `vscode_settings.json` — VS Code workspace settings
- `.eslintrc.json` — ESLint configuration
