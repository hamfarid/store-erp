# 🚀 دليل البدء السريع - نظام Gaara ERP

## 📋 المحتويات
- [متطلبات النظام](#متطلبات-النظام)
- [التشغيل السريع](#التشغيل-السريع)
- [الوصول للنظام](#الوصول-للنظام)
- [الأوامر المفيدة](#الأوامر-المفيدة)
- [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🔧 متطلبات النظام

### الحد الأدنى:
- **Python:** 3.11 أو أحدث
- **الذاكرة:** 4 GB RAM
- **المساحة:** 2 GB مساحة فارغة
- **نظام التشغيل:** Windows 10/11, Ubuntu 20.04+, macOS 10.15+

### المستحسن:
- **Python:** 3.12
- **الذاكرة:** 8 GB RAM
- **المساحة:** 10 GB مساحة فارغة
- **المعالج:** 4 أنوية أو أكثر

---

## ⚡ التشغيل السريع

### 1. تشغيل النظام (طريقة واحدة)
```bash
python start_system.py
```

**هذا كل شيء!** 🎉

السكريبت سيقوم بـ:
- ✅ فحص المتطلبات
- ✅ تفعيل البيئة الافتراضية
- ✅ إعداد قاعدة البيانات
- ✅ إنشاء مستخدم إداري
- ✅ تشغيل الخادم
- ✅ تشغيل الواجهة الأمامية

### 2. للإنتاج
```bash
python start_system.py --production --port 8000
```

---

## 🌐 الوصول للنظام

بعد التشغيل الناجح، ستحصل على:

### 🔗 الروابط:
- **الواجهة الأمامية:** http://localhost:5173
- **لوحة الإدارة:** http://localhost:9551/admin
- **API:** http://localhost:9551/api

### 👤 بيانات تسجيل الدخول الافتراضية:
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123`

⚠️ **مهم:** غيّر كلمة المرور فور تسجيل الدخول!

---

## 🛠️ الأوامر المفيدة

### إدارة النظام:
```bash
# إعداد النظام الأولي
python system_config.py setup

# فحص صحة النظام
python system_config.py check

# مراقبة النظام المباشرة
python system_monitor.py

# نسخ احتياطي شامل
python backup_system.py backup --type full

# عرض النسخ الاحتياطية
python backup_system.py list

# تنظيف النسخ القديمة
python backup_system.py cleanup
```

### إدارة Django:
```bash
cd gaara_erp

# إنشاء مستخدم إداري جديد
python manage.py createsuperuser

# تطبيق الترحيلات
python manage.py migrate

# جمع الملفات الثابتة
python manage.py collectstatic

# تشغيل الاختبارات
python manage.py test

# فحص النظام
python manage.py check
```

### إدارة قاعدة البيانات:
```bash
cd gaara_erp

# عرض حالة الترحيلات
python manage.py showmigrations

# إنشاء ترحيل جديد
python manage.py makemigrations

# تصدير البيانات
python manage.py dumpdata > backup.json

# استيراد البيانات
python manage.py loaddata backup.json

# فتح shell Django
python manage.py shell
```

---

## 🔍 استكشاف الأخطاء

### المشاكل الشائعة:

#### 1. خطأ "Python not found"
```bash
# تأكد من تثبيت Python 3.11+
python --version

# أو
python3 --version
```

#### 2. خطأ "Virtual environment not found"
```bash
# إنشاء بيئة افتراضية جديدة
python -m venv .venv

# تفعيل البيئة (Windows)
.venv\Scripts\activate

# تفعيل البيئة (Linux/macOS)
source .venv/bin/activate

# تثبيت المتطلبات
pip install -r requirements.txt
```

#### 3. خطأ في قاعدة البيانات
```bash
cd gaara_erp

# إعادة تعيين قاعدة البيانات
python manage.py migrate --run-syncdb

# أو حذف قاعدة البيانات وإعادة إنشائها
rm db.sqlite3
python manage.py migrate
python manage.py createsuperuser
```

#### 4. خطأ في المنافذ (Port already in use)
```bash
# تشغيل على منفذ مختلف
python start_system.py --port 8080

# أو إيقاف العملية المستخدمة للمنفذ (Windows)
netstat -ano | findstr :9551
taskkill /PID <PID_NUMBER> /F

# Linux/macOS
lsof -ti:9551 | xargs kill -9
```

#### 5. مشاكل الواجهة الأمامية
```bash
cd main-frontend

# تثبيت المتطلبات
npm install

# تشغيل الواجهة
npm run dev

# بناء للإنتاج
npm run build
```

### 📊 فحص حالة النظام:
```bash
# فحص شامل للنظام
python system_config.py check

# مراقبة مباشرة
python system_monitor.py --interval 10

# فحص Django
cd gaara_erp
python manage.py check --deploy
```

### 📝 السجلات:
```bash
# عرض سجلات النظام
tail -f gaara_erp/logs/gaara_erp.log

# عرض سجلات Django
cd gaara_erp
python manage.py shell -c "
import logging
logger = logging.getLogger('django')
logger.info('Test log message')
"
```

---

## 🎯 الخطوات التالية

بعد التشغيل الناجح:

### 1. تخصيص النظام:
- [ ] تغيير كلمة مرور المدير
- [ ] إعداد معلومات الشركة
- [ ] تكوين البريد الإلكتروني
- [ ] إعداد النسخ الاحتياطية

### 2. إضافة البيانات:
- [ ] إنشاء المستخدمين
- [ ] إعداد الأقسام
- [ ] إدخال بيانات الموظفين
- [ ] تكوين المنتجات والخدمات

### 3. التدريب:
- [ ] قراءة دليل المستخدم الكامل
- [ ] تدريب الفريق
- [ ] اختبار العمليات
- [ ] إعداد النسخ الاحتياطية

---

## 📚 المراجع

### الأدلة الكاملة:
- **[دليل المستخدم](USER_GUIDE.md)** - دليل شامل لاستخدام النظام
- **[دليل النشر](DEPLOYMENT_GUIDE.md)** - دليل النشر للإنتاج
- **[دليل المطور](DEVELOPER_GUIDE.md)** - دليل التطوير والتخصيص

### الملفات المهمة:
- `start_system.py` - سكريبت التشغيل الرئيسي
- `system_config.py` - إدارة التكوين
- `system_monitor.py` - مراقبة النظام
- `backup_system.py` - النسخ الاحتياطية
- `.env` - متغيرات البيئة

### الدعم:
- **البريد الإلكتروني:** support@gaara-erp.com
- **الموقع:** www.gaara-erp.com
- **التوثيق:** docs.gaara-erp.com

---

## ⚡ نصائح للأداء

### تحسين الأداء:
```bash
# تفعيل ضغط الملفات
export ENABLE_COMPRESSION=True

# تحسين قاعدة البيانات
cd gaara_erp
python manage.py optimize_db

# تنظيف الملفات المؤقتة
python system_config.py cleanup
```

### مراقبة الأداء:
```bash
# مراقبة مستمرة
python system_monitor.py --interval 30

# تقرير الأداء
python system_monitor.py --report
```

---

## 🎉 تهانينا!

نظام Gaara ERP جاهز للاستخدام! 🚀

للحصول على أفضل تجربة:
1. اقرأ دليل المستخدم الكامل
2. قم بإعداد النسخ الاحتياطية
3. درب فريقك على النظام
4. ابدأ بإدخال البيانات تدريجياً

**نتمنى لك تجربة ممتازة مع نظام Gaara ERP! 🎯**
