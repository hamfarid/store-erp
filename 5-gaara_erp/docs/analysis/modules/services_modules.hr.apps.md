# services_modules.hr.apps

## Imports
- django.apps
- django.utils.translation

## Classes
- HrConfig
  - attr: `default_auto_field`
  - attr: `name`
  - attr: `verbose_name`
  - method: `ready`

## Functions
- ready

## Class Diagram

```mermaid
classDiagram
    class HrConfig {
        +default_auto_field
        +name
        +verbose_name
        +ready()
    }
```
