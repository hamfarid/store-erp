# business_modules.inventory.apps

## Imports
- django.apps

## Classes
- InventoryConfig
  - attr: `default_auto_field`
  - attr: `name`
  - attr: `verbose_name`
  - method: `ready`

## Functions
- ready

## Class Diagram

```mermaid
classDiagram
    class InventoryConfig {
        +default_auto_field
        +name
        +verbose_name
        +ready()
    }
```
