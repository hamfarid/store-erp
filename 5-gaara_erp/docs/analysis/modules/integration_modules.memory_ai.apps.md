# integration_modules.memory_ai.apps

## Imports
- django.apps

## Classes
- MemoryAiConfig
  - attr: `default_auto_field`
  - attr: `name`
  - attr: `verbose_name`

## Class Diagram

```mermaid
classDiagram
    class MemoryAiConfig {
        +default_auto_field
        +name
        +verbose_name
    }
```
