# مثال: Simple API Project

هذا مثال بسيط لمشروع API تم إنشاؤه باستخدام `setup_project_structure.sh`.

## البنية

```
simple-api/
├── docs/              # التوثيق الكامل
├── src/
│   ├── backend/      # كود API
│   └── shared/       # الكود المشترك
├── tests/            # الاختبارات
└── todo/             # تتبع المهام
```

## الإعداد

```bash
# إنشاء المشروع
bash <(curl -s https://raw.githubusercontent.com/hamfarid/global/main/download_and_setup.sh) simple-api

# الانتقال إلى المشروع
cd simple-api

# تثبيت التبعيات (Python مثال)
python3 -m venv .venv
source .venv/bin/activate
pip install fastapi uvicorn

# تشغيل API
uvicorn src.backend.main:app --reload
```

## الميزات

- **هيكل منظم**: جميع الملفات في أماكنها الصحيحة
- **توثيق شامل**: 17 ملف توثيق جاهز
- **تتبع المهام**: نظام todo منظم
- **معايير OSF**: جاهز لتطبيق معايير الأمان والجودة

## الخطوات التالية

1. راجع `docs/TODO.md` وأضف المهام
2. حدّث `docs/API_Contracts.md` بـ endpoints
3. ابدأ بتطوير الوحدات الأقل اعتمادية
4. وثّق القرارات في `docs/Solution_Tradeoff_Log.md`

## OSF_Score

جميع القرارات التقنية في هذا المشروع تتبع معايير OSF:

```
OSF_Score = (0.40 × Security) + (0.25 × Correctness) + (0.15 × Reliability) + 
            (0.10 × Maintainability) + (0.05 × Performance) + (0.05 × Speed)
```

الأمان والجودة أولاً! 🔒

